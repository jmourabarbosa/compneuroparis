Scholar-link helper files live here so the repository root stays focused on the app itself.

Contents:
- `find-scholar-links.js`
- `find-scholar-links.py`
- source and batch JSON files
- missing-entry text reports

Run the scripts from this directory so their relative file paths continue to work.
